<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen_primary"
    android:fillViewport="true"
    tools:context=".ui.rinse.RinseFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="720dp"
        android:orientation="vertical"
        android:padding="24dp">

        <include
            android:id="@+id/rinseHeader"
            layout="@layout/include_albers_header" />

        <LinearLayout
            android:id="@+id/rinseTopActions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/backButton"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="@drawable/bg_button_secondary"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:text="Back"
                android:textColor="@color/text_primary"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/helpButton"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_marginStart="12dp"
                android:layout_weight="1"
                android:background="@drawable/bg_button_secondary"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:text="Help"
                android:textColor="@color/text_primary"
                android:textStyle="bold" />

        </LinearLayout>

        <LinearLayout
            android:id="@+id/rinseActionCard"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:background="@drawable/bg_card_rounded"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp">

            <ImageView
                android:id="@+id/rinseIcon"
                android:layout_width="92dp"
                android:layout_height="92dp"
                android:contentDescription="Rinse sanitize"
                android:src="@drawable/ic_rinse_sanitize" />

            <TextView
                android:id="@+id/rinseTitleText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:gravity="center"
                android:text="Rinse / Sanitize"
                android:textColor="@color/text_primary"
                android:textSize="24sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/rinseTimerText"
                android:layout_width="match_parent"
                android:layout_height="74dp"
                android:layout_marginTop="18dp"
                android:background="@drawable/bg_timer_window"
                android:gravity="center"
                android:text="00:60"
                android:textColor="@color/text_primary"
                android:textSize="36sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/startRinseButton"
                android:layout_width="match_parent"
                android:layout_height="58dp"
                android:layout_marginTop="22dp"
                android:background="@drawable/bg_button_green"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:text="START CLEANING CYCLE"
                android:textColor="@color/text_primary"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/emergencyStopRinseButton"
                android:layout_width="match_parent"
                android:layout_height="58dp"
                android:layout_marginTop="12dp"
                android:background="@drawable/bg_button_red"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:text="EMERGENCY STOP"
                android:textColor="@color/text_primary"
                android:textStyle="bold" />

        </LinearLayout>

        <TextView
            android:id="@+id/rinseAvailabilityText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="18dp"
            android:background="@drawable/bg_warning_box"
            android:padding="16dp"
            android:text="Rinse/Sanitize is disabled if both pumps are unavailable."
            android:textColor="@color/text_secondary" />

        <TextView
            android:id="@+id/systemStatusButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/bg_button_secondary"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:text="System Status"
            android:textColor="@color/text_primary"
            android:textStyle="bold" />

    </LinearLayout>

</ScrollView>
